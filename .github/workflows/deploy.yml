name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install sshpass
        run: sudo apt-get update && sudo apt-get install -y sshpass

      - name: Build and deploy on server
        env:
          SSH_PASSWORD: ${{ secrets.SSH_PASSWORD }}
          SERVER: ${{ secrets.SERVER }}
          USERNAME: ${{ secrets.USERNAME }}
        run: |
          sshpass -p "$SSH_PASSWORD" ssh -o StrictHostKeyChecking=no $USERNAME@$SERVER << 'EOF'
            # Build the project
            cd ./client/jsClient
            git pull origin main
            yarn install
            yarn build
          
            # Move built files to deployment directory
            sudo mv dist/* /var/www/duk

            # Copy Nginx config
            sudo cp client/jsClient/nginx/nginx.conf /etc/nginx/sites-available/duk
          
            # Set up Nginx
            sudo ln -sf /etc/nginx/sites-available/duk /etc/nginx/sites-enabled/
            sudo nginx -t
            sudo systemctl restart nginx
          EOF